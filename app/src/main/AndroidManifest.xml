<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.noteapp">

    <!-- Quyền truy cập đa phương tiện (nếu bạn cần lưu/truy xuất ảnh, âm thanh...) -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO"/>
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO"/>
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/> <!-- ⏰ Đặt giờ chính xác -->

    <!-- Nếu hỗ trợ Android < 13 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NoteApp"
        tools:targetApi="31">

        <!-- Launcher activity -->
        <activity android:name=".MainActivity"/>

        <!-- Các activity trong app -->
        <activity android:name=".HomeActivity" android:exported="true" />
        <activity android:name=".AddNoteActivity" android:exported="true" />
        <activity android:name=".EditNoteActivity" android:exported="true" tools:node="merge" />

        <!-- Receiver để nhận lời nhắc và hiện thông báo -->
        <receiver android:name="com.example.noteapp.models.ReminderReceiver" android:exported="false" />

        <activity android:name=".models.PinActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>



    </application>
</manifest>
